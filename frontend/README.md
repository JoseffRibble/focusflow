# FocusFlow Frontend

This is a minimal React frontend for FocusFlow.

## Features

- Login via `/api/login/`
- View task list via `/api/tasks/`
- Create new task

## How to Run

```bash
cd frontend
npm install
npm run dev
```
